settings:
  host: "127.0.0.1:9097"
  log_level: "INFO"
  # The owner reads this file from the admin, under Status → Expert mode: the
  # shop is a daemon, and without it the log reaches only the machine.
  log_path: "~/.fastoshop/fastoshop.log"
  database: "~/.fastoshop/fastoshop.db"
  base_url: "http://localhost:9097"
